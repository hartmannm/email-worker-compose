version: '3'
volumes:
    dados:
services:
    db: 
        image: postgres:9.6
        volumes:
            - dados:/var/lib/postgressql/data
            - ./scripts:/scripts
            - ./scripts/init.sql:/docker-entrypoint-initdb.d/init.sql